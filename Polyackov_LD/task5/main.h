#pragma once

#include <iostream>
#include <iomanip> //Äëÿ setw
#include <Windows.h>
#include <fstream>
#include <ctime>
#include <limits>
#include <string>
#include <vector>
#include <conio.h>

#pragma warning(disable:4996)

using namespace std;

struct Name
{
    string surname;
    string name;
    string patronymic;
};

/*enum ConsoleColor {
    Black = 0,
    Blue = 1,
    Green = 2,
    Cyan = 3,
    Red = 4,
    Magenta = 5,
    Brown = 6,
    LightGray = 7,
    DarkGray = 8,
    LightBlue = 9,
    LightGreen = 10,
    LightCyan = 11,
    LightRed = 12,
    LightMagenta = 13,
    Yellow = 14,
    White = 15
};
*/
